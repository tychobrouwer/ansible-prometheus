[Unit]
After=network.service

[Service]
WorkingDirectory=
ExecStart=pve_exporter --config.file /etc/prometheus/pve-exporter.yml --web.listen-address={{ prometheus_pve_exporter_address }}:{{ prometheus_pve_exporter_port }}
Restart=always

[Install]
WantedBy=default.target
