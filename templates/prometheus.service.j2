[Unit]
After=network.service

[Service]
WorkingDirectory=
ExecStart=prometheus --config.file /etc/prometheus/prometheus.yml --storage.tsdb.retention.time={{ prometheus_retention }}
Restart=always

[Install]
WantedBy=default.target
